import React from 'react';
import {Link} from "react-router-dom";
import BasicLayout from "../layouts/BasicLayout";

function Main(props) {
    return (
        <BasicLayout>
            <div>Main Page</div>
        </BasicLayout>
    );
}

export default Main;